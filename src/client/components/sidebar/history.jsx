/**
 * history select
 */

import {memo} from 'react'
import ItemList from '../setting-panel/list'

export default memo(() => {
  return (
    <div className="history-panel">
      <ItemList
      />
    </div>
  )
})
