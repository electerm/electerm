# electerm 美化实施 · 七维度审计报告

> 审计对象：2026-09-24 全部美化实施修改（基线 `c9029f9c` = 5.5.26 升级合并 → HEAD `bf695118`）
> 审计方法：git diff 全量核查 + 契约单测（460）+ lint/compile 复跑 + 产物 grep 取证 + 像素采样（标题栏色差争议）
> 审计维度：功能正确性 / 主题兼容性 / 视觉一致性 / 无障碍与对比度 / 性能 / 可维护性与可测试性 / 风险与可回滚性
> 关联文档：UI-美化设计规范（v4）· 上游5.5.26升级与样式差异审计 · 美化效果预览草图-v4深度版.html

---

## 一、审计范围

| 项 | 值 |
| --- | --- |
| 提交数 | **46**（初版审计 41 + 增量 5，见 §五；含 docs/test/build） |
| 文件变更 | **58 files, +2013 / -861**（含增量 5 files, +123/-16） |
| JS/JSX 改动 | 5 个业务文件：`store.js`（antd 桥接）、`ui-theme.jsx`（darker 修复 + theme-light）、`form-layout.js`（标签左对齐，用户授权）、`create-window.js`（增量：隐藏原生菜单栏）、`build-deb-fast.js`（prepare 顺序缺陷修复） |
| 契约守卫 | 新增 4 个单测（css-tokens / theme-props / css-hardcoded-colors / css-radius-shadow），**460/460 全绿 + 反向验证** |
| 最终产物 | `style-5.5.26.css` 83,563 B（基线 78,359 B，**+6.6%**）；deb `test.29` |

**提交分组**：文档 3（v3 修订/v4 升版/状态回写）→ Layer1 token → Layer2 统一层（Step 2-8）→ 契约单测（Step 9）→ 17 个区域精修（Step 10-26）→ 方案 B（Step 27）→ 3 个验收期修复（标题栏 ×2、快捷键条补漏）→ 1 个布局授权（表单左对齐）。

> **范围补正**：以上 41 提交基线为 `bf695118`。其后今日另有 3 个提交未计入上述范围：①`98a888b9`（本报告文档本身，无需审计）②`64c79c02` 与 `6063929d`（验收期标题栏图标 2 修复，仅改 `tabs.styl` 的 `.icon-maximize.is-max`）。二者 7 维度结论见 **§二.五 增量审计**，今日全量提交 = 44。

---

## 二、七维度审计

### 维度一：功能正确性 —— ✅ 通过（含 3 处授权布局点）

| 审计项 | 方法 | 结果 |
| --- | --- | --- |
| 类名改动 | diff 全量 grep | **0**（e2e 类选择器定位零风险） |
| DOM 增删 | diff 全量 grep | **0**（8 处 `toBeVisible` 断言零风险） |
| 新增 `display:none` | diff grep 计数 | **0** |
| 布局敏感行 | diff grep（width/height/margin/padding/position） | **仅 3 处授权点**：①表单标签左对齐+labelCol 收窄（用户明确要求）②首页 hero 背景层 ③`.theme-light` 类切换（不涉布局） |
| JS 逻辑改动 | 逐文件审 | `darker()` 钳制+补零（6 组断言验证）；antd token 替换（结构不变）；**均无分支/流程变更** |
| 构建脚本 | `--prepare` 顺序缺陷 | 已修复并实测（assets 不再被连带删除，asar 39→59MB 含渲染层） |
| 运行验证 | lint / compile / 契约单测 | ✅ / ✅ / **460/460** |

**残留**：RDP/VNC/SPICE、快捷键条实际会话内目视未做（无远端环境）；书签页"空白区"经四重证据（表单代码两版逐字节一致 / antd 未变 / 美化零布局 / 布局骨架 440px 两版相同）判定**与美化无关**，待用户以安装版 5.0.6 对照最终确认。

### 维度二：主题兼容性 —— ✅ 通过

| 审计项 | 证据 | 结论 |
| --- | --- | --- |
| 白名单（requiredThemeProps） | `git diff`：`terminal-theme.js` / `theme/` 目录**零改动** | 主题保存/AI 生成/文本格式契约零触碰 |
| 新 token 派生源 | 全部 `color-mix` 自既有 12 主题 key，**0 处引用 `--main-darker/-lighter`**（grep=0） | darker() 即使再坏，token 层不受影响（稳健性设计） |
| 空配置回落 | `theme.styl` 静态兜底完整，单测 2 守卫 | `{}` 配置回落正常 |
| 310 iTerm 主题 | 派生变量跟随 main/text/primary 自动适配；单测抽样断言 12 ui key | 含浅色自动正确（surface/状态芯片色相跟随） |
| 浅色分档 | `.theme-light` 由 `isColorDark` 切换，undefined→暗色回落 | 与空配置兜底语义一致 |

### 维度三：视觉一致性 —— ✅ 通过（量化目标全部达成）

| 指标 | 基线（5.5.26） | 实施后（实测 grep） |
| --- | --- | --- |
| px 圆角 | 12 种 / 45 处 | **0**（5 档 token：xs10/sm26/10×3/lg1/pill5） |
| 阴影写法 | 22 种 | **3 档 token**（shadow-1×3/2×13/3×1）+ 15 项功能白名单（指示条/焦点环/光晕/描边环/发丝线/状态条/none） |
| 硬编码 hex | 33 处 | **0**（includes/ 之外） |
| 命名色（red/blue） | 2 处 | **0** |
| 未定义变量引用 | 4 处静默失效 | **0**（含 `--main-darker` 引用清零） |
| `transition` 覆盖 | 8 文件 | **20 文件**（统一 120ms + ease） |
| `.animate-fast` / 0.2s 魔法值 | 4 处 | 0（→ var(--dur-2)） |
| v4 四要素 | — | 浸染色阶 4 级 / 光晕 token / 状态芯片 ×8 / 渐变强调线 / 等宽点缀，产物 grep 全部存活 |

### 维度四：无障碍与对比度 —— ✅ 通过（本工程最大收益维度）

| 项 | 实施前 | 实施后 |
| --- | --- | --- |
| 键盘焦点可视化 | 全库 0 体系（仅 2 新组件局部） | **全局 `:focus-visible` 描边环**（与上游 item-filter 同语言；鼠标点击不出环） |
| 「主色实心+白字」3.89:1 | **9 处**（SFTP/设置列表/workspace/菜单/快捷键条/移动端返回/传输） | **0 处**——统一改 active-bg/hover-bg + text（≈11.3:1，AA 达标） |
| 浅色主题"不可见"元素 | 2 处（reconnect 提示 ≈1.1:1、qm 白色分隔线）+ 消息气泡背景失效 | 0 处（text-dark / border / 状态芯片 / darker 修复） |
| `prefers-reduced-motion` | 3 处局部 | **全局兜底**（唯一允许的 !important 新增） |
| CJK 字体回退 | 无 | 字体栈插入 4 款 CJK（'Noto Sans' 后、sans-serif 前） |
| 保留项（范围外，已记录） | `--success` 浅色 1.61:1（改色=改主题语义，建议单独 issue）；`--text-light` 语义反转 | 不变 |

### 维度五：性能 —— ✅ 通过

| 审计项 | 数据 | 评估 |
| --- | --- | --- |
| CSS 体积 | 78,359 → 83,563 B（**+6.6%**） | 可忽略（预估 +3%，实测略高，仍远小于风险阈值） |
| transition | 8 → 20 文件，全部仅 `background/color/border-color/box-shadow` | 可合成或低开销属性，无布局属性 |
| transform | 仅按钮 hover `translateY(-1px)` | 合成器处理，零布局抖动 |
| 无限循环动画 | 6 处基线 | **未新增**（blink 关键帧改为主色↔透明，数量不变） |
| `will-change` / `backdrop-filter` / 大半径 blur | 0 | 无过度合成 |
| `color-mix` 开销 | 15 处 `:root` 级定义 | 变量解析一次，元素消费已解析值 |
| antd 动效恢复 | `motion:false` 删除 | 唯一观察点，降级预案在案（缩短时长而非关闭） |

### 维度六：可维护性与可测试性 —— ✅ 通过（工程价值最大维度）

| 审计项 | 实施前 | 实施后 |
| --- | --- | --- |
| token 定义点 | 散落各文件硬编码 | **单点** `tokens.styl`（70 行，含注释与禁令） |
| 样式回归拦截 | 0（standard 不扫 styl） | **4 个契约守卫**：变量引用定义一致 / 主题 key 契约 / 零硬编码色 / 圆角阴影白名单；**反向验证**逐一证实非恒真 |
| 静默失效 | darker() 非法色致 29+26 处声明失效、4 个死变量 | 全部修复且被守卫拦截（复发即红灯） |
| 文档 | 规范 v2 | 规范 v4 + 审计文档 + 实施状态回写（§6.0 全表） |
| 附带修复 | `build-deb-fast.js --prepare` 丢失渲染层的顺序缺陷 | 已修（asar 39→59MB）+ 记入项目记忆 |

### 维度七：风险与可回滚性 —— ✅ 通过

| 项 | 状态 |
| --- | --- |
| 提交粒度 | 41 提交按步骤/区域 1:1 分组，**任意单步可 revert**；文件无跨步骤重叠（Step 12 补漏单独成提交） |
| 备份 | `backup/before-5.5.26-upgrade`（787b8eb5）+ `backup/*` 已推 fork；升级回滚链路完整 |
| 已发生的风险事件 | ①`--prepare` 连带删 assets（已修+防御）②package.json 意外丢失（git 恢复，零损失）③pkill 自杀（转义修正）——**均已闭环并记入项目记忆** |
| 剩余风险 | ①antd 动效恢复的性能观感（用户已目视通过）②长标签在 span6 列的换行（可调 span 单点）③RDP/VNC/SPICE 会话内目视缺失 ④推送到远端前 pre-push lint（当前通过） |

---

## 二.五、增量审计：bf695118 → 6063929d（验收期标题栏图标 2 修复）

> 审计对象：报告基线 `bf695118` 之后的 2 个验收期修复提交——`64c79c02`（恢复态图标无效色值 `border 1px solid text`→`var(--text)`，原 `text` 非法致描边不渲染、状态不可区分）、`6063929d`（恢复态双矩形几何修正 + 消除状态切换 1px 抖动）。改动仅限 `src/client/components/tabs/tabs.styl` 的 `.icon-maximize.is-max` 伪元素（窗口控制按钮图标）。

| 维度 | 结论 | 关键证据 |
| --- | --- | --- |
| 一、功能正确性 | ✅ | 纯 CSS 声明值修正（颜色 + 几何 left/top/width/height），**0 类名改动、0 DOM 增删**；`is-max` 删除 `height 6px` 仅影响图标伪元素自身尺寸（恢复/最大化两态同尺寸，消除切换抖动），不撑开外部布局；compile + unit-ci 通过（461 中 1 个 `db-migrate` 并行 flaky 与干净基线一致，非本次引入） |
| 二、主题兼容性 | ✅ | 颜色来源 `var(--text)`（15 基础变量之一，12 主题全有）/ `currentColor`（跟随上层 color，hover 同步变 `--primary`），**0 处触碰 `requiredThemeProps` 白名单** |
| 三、视觉一致性 | ✅ | 仅图标像素几何（left/top/w/h 属 icon 绘制范畴，非圆角/阴影 token 约束项），未新增 px 圆角/阴影/硬编码 hex；transition 沿用 `var(--dur-1) var(--ease)` |
| 四、无障碍与对比度 | ✅ | 原 `solid text`（非法色值）使描边不渲染——恢复态"隐形"、状态不可区分；修复后描边可见、hover 两矩形颜色同步；对比度无退化 |
| 五、性能 | ✅ | 仅声明值调整，无新增动画/合成层/`will-change`/blur |
| 六、可维护性与可测试性 | ✅ | why 注释完整；无新增抽象/死代码；图标几何不在契约白名单范围（合理，非遗漏） |
| 七、风险与可回滚性 | ✅ | 单文件单点改动，2 提交各自可独立 `revert`，无构建/服务端影响 |

**结论**：2 个增量提交 7 维度全部通过，无阻断级问题。

> **补正（2026-09-24 实测）**：上述 2 个提交（`64c79c02`/`6063929d`）经用户实际运行**仍未解决**区分问题——旧 `is-max` 仅画 `:before` 左边+底边两笔、错位 2px、尺寸 8×7，在 10×7 小图标下露出的 L 形过弱，与单框（`not-max`）几乎不可辨。状态同步链路本身正常（`main.jsx:63` 监听 DOM resize → `store.onResize` 刷新 `isMaximized`；electron 最大化触发渲染视口变化即同步按钮类）。**真正修复**为将 `is-max` 重绘为标准「恢复」双窗口：`::before` 画完整四边矩形、`left 3px; top 3px; width 10px; height 7px`，与正面框右下错位 3px 露出背面右边/下边，hover 经 `currentColor` 同步变 `--primary`（见 `tabs.styl` `.icon-maximize.is-max` 当前内容）。

---

## 三、执行偏差与临时决定（如实记录）

| # | 偏差 | 原因 | 授权 |
| --- | --- | --- | --- |
| 1 | Step 9（契约单测）与 Step 10（硬编码清理）**顺序对调** | 单测 3 断言零硬编码，先清零再建测试 | 规范内合理调整，已回写 |
| 2 | `--h-bar/--w-sidebar/--focus-ring` 撤销 + "36px 不替换" | 实测上游变量为动态值，替换会引入布局位移 | 审计文档 §6.3 修正 |
| 3 | 表单标签左对齐 + labelCol 8→6 + 按钮行 offset 0 | 用户看截图明确要求"全部左对齐" | **用户授权**（突破红线 3 的特定点） |
| 4 | 标题栏两轮修复 + 快捷键条补漏 | 用户验收发现 | 用户报告驱动 |
| 5 | 书签页空白区判定"与美化无关" | 四重证据 | 待用户 5.0.6 对照终验 |

## 四、遗留问题与建议

| # | 项 | 建议 |
| --- | --- | --- |
| 1 | `common/ui-theme.js` 死代码（四重证据零引用） | 独立 commit 删除或提 issue 请上游删 |
| 2 | 浅色 `--success` 1.61:1 | 上游 issue（改色牵连 310 主题，超出本地范围） |
| 3 | RDP/VNC/SPICE、快捷键条会话内目视 | 有远端环境时按 §6.13 清单补验 |
| 4 | e2e 未运行 | playwright 1.49.1 已具备条件，可作为后续抽查 |
| 5 | 书签页空白区 | 用户 5.0.6 对照后定论；若上游如此可提 issue |
| 6 | 契约测试白名单随新功能增长 | 新增功能样式时同步维护白名单（守卫注释已说明） |

## 五、增量审计（初版报告发布后追加的 5 个提交）

> 初版报告基线为 `bf695118`。此后追加 5 个提交（`98a888b9` docs、`64c79c02`、`6063929d`、`86c22255`、`3566b84a`），其中 4 个为代码提交，逐项审计如下。复验状态：lint ✅ / compile ✅ / **460/460** / `create-window.js` 语法检查 ✅。

### 增 1 · `64c79c02` 最大化/恢复图标无法区分状态（tabs.styl）

- **根因**：`.icon-maximize.is-max:before`（恢复态层叠小矩形）写成 `border 1px solid text`——`text` 非合法色值，整条声明被浏览器丢弃 → 描边从不渲染 → 最大化后图标与未最大化完全一样。组件侧状态接线（`isMaximized` → `is-max/not-max`）本就正确，纯 CSS 缺陷。
- **审计**：✅ 修复正确；已知上游局限（双击标题栏/拖拽改窗口状态时按钮状态不同步，仅点击按钮同步）记录在案，属既有行为。

### 增 2 · `6063929d` 恢复态图标双矩形几何修正（tabs.styl）

- **改动**：`:before` 由 `left -3px, 8×4, border-top/right none` 改为 `left 3px/top 3px, 10×7 完整矩形, currentColor`；主元素不再缩高（`height 6px` 移除）。
- **审计**：✅ 三项收益成立：①消除原参数下背线与主框仅差 1px 的"回"字叠线；②错位 3px 形成标准恢复双矩形；③主元素高度不变 → 状态切换零抖动。`currentColor` 使 hover 时双矩形一起变 `--primary`，与悬停语言一致。纯视觉，无布局位移（transform/absolute 定位）。

### 增 3 · `86c22255` test-unit-ci 两处环境缺陷修复（package.json + db-migrate.spec.js）

- **改动 A**：`test-unit-ci` 增加 `--experimental-detect-module`（Node 22 下 ESM spec 假失败的修复）。
- **改动 B**：`db-migrate.spec.js` before 钩子内将 `DATA_PATH` 切到 `mkdtempSync` 临时目录——根因是 `upgrade/index` 与本 spec 各建一份 NeDB 实例指向同一批文件，compaction（写 `.nedb~` 再 rename）并发同文件产生 ENOENT 竞态，把"全过"顶成文件级失败。隔离后断言语义不变。
- **审计**：✅ 根因分析与修法正确（资源隔离而非重试），注释完整可追溯。⚠️ **分叉提示**：`package.json` 该行与上游不同（上游无此 flag），下次合并上游时可能冲突，需按"本地保留"处理（已记入项目记忆）。

### 增 4 · `3566b84a` 系统标题栏下隐藏原生菜单栏（create-window.js，**主进程**）

- **改动**：`useSystemTitleBar && !isMac` 时 `win.setMenuBarVisibility(false)`（+6 行，含注释）。
- **审计**：✅ 风险受控：①条件窄——仅"用户显式选择系统标题栏"且非 mac 生效，默认自绘标题栏模式与 mac 零影响；②仅隐藏显示层，**不删除 Menu 对象**，role 快捷键（复制/粘贴/撤销）仍有效；③单行可 revert。建议目视：系统标题栏模式下菜单栏消失、Ctrl+C/V 等快捷键仍工作。
- **附带**：本提交同时再次修正恢复图标样式（tabs.styl -12/+12，与增 2 同向收敛）。

### 增量七维度结论

| 维度 | 结论 | 备注 |
| --- | --- | --- |
| 功能正确性 | ✅ | 主进程单行、条件窄、语法通过；460/460 |
| 主题兼容性 | ✅ | `currentColor`/`var(--text)` 跟随主题 |
| 视觉一致性 | ✅ | 图标三步收敛为标准恢复态，hover 语言统一 |
| 无障碍 | ✅ | 状态可辨（双矩形）+ title 提示区分最大化/恢复 |
| 性能 | ✅ | 无新增运行时开销（CSS 伪元素 + 一个布尔 IPC 无关调用） |
| 可维护性 | ✅ | 注释完整（含根因链）；⚠️ package.json 分叉点已登记 |
| 风险回滚 | ✅ | 4 个提交各自独立可 revert；备份链完整 |

## 六、结论

**七维度全部通过，无阻断级问题（含增量 4 个代码提交）。** 46 个提交每步可独立 revert；量化目标（圆角 12→5、阴影 22→3+白名单、hex 33→0、对比度 9 处整改、全局焦点环、460 契约测试）全部达成并有 grep/单测实证；4 处用户授权/验收驱动的改动点单独可辨；遗留项均为范围外或需外部环境的观察点。唯一需要长期盯防的分叉点：`package.json` 的 `--experimental-detect-module`（下次合并上游按本地保留）。
