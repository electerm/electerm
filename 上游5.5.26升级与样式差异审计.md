# 上游 5.5.26 升级方案与样式差异审计

> 目的：本地代码停留在上游 **5.0.6**，官方已到 **5.5.26**（领先 **215 个提交**）。
> 决定「先升级再美化」后，本文档回答两个问题：**升级会不会出问题**、**升级后美化方案要怎么改**。
> 审计方法：本地已完成 `upstream/master` 全量 fetch，全部结论由 `git diff / merge-tree / grep / numstat` 实测得出。

---

## 一、结论摘要（TL;DR）

> ✅ **升级已于 2026-09-23 执行完成并验证通过**，HEAD = `c9029f9c`（merge commit），已推送 fork。
> 实际执行结果与环境要求见 **§十三**；本文档 §二/§六 中与实测不符的 3 处已就地更正。

| 问题 | 结论 |
| --- | --- |
| 升级会不会很难？ | **不难**。479 个文件变更、+32415/-10227 行，但**只有 2 个文件冲突**（§二） |
| 会不会破坏美化方案？ | **不会**。美化方案的 4 个技术地基（变量系统、主题白名单、类名、构建配置）**全部零变化**（§三） |
| 会不会破坏主题兼容性？ | **不会**。33 个主题 key 零增删改，310 个 iTerm 主题 + 用户自建主题可直接沿用（§三） |
| 美化方案的核心价值还在吗？ | **还在**。`--border` 仍未定义、硬编码色仍 33 处、`!important` 反增到 46 处、无全局焦点环（§八） |
| 需要改多少方案内容？ | **1 条迁移 + 6 处行号重算 + 9 个新区域纳入 + 2 个变量名修正**（§六、§七） |
| 最大的单向风险 | RDP/VNC/SPICE 外壳样式被抽到新文件 `common/remote-session-shell.styl`，方案 §4.13 必须改作用对象（§四.1） |
| 实际结果 | **13 项预测全部命中**：2 处冲突、测试路径迁移、playwright 升级、`btest`/`bdebfast` 自动保留；唯 1 处偏差 = `tabs.styl` 行数（§六.2） |

**一句话**：升级是低风险动作，美化方案在 5.5.26 上依然成立，只是**行号要重算、新增 9 个区域要补进来**。

---

## 二、升级冲突预演（`git merge-tree` 内存试算）

用 `git merge-tree --write-tree --name-only HEAD upstream/master` 在内存中做了一次真实合并，**不触碰工作区**：

**升级规模**：

```
479 files changed, 32415 insertions(+), 10227 deletions(-)
```

**冲突结果：仅 2 个文件**

| 文件 | 冲突原因 | 解决方式 |
| --- | --- | --- |
| `.gitignore` | 双方都在文件末尾追加 | **两边都保留**（见下方明细） |
| `src/client/components/tabs/tabs.styl` | 双方都在同一区域新增 | **两边都保留**（见下方明细） |
| `package.json` | — | ✅ **自动合并成功**，无冲突 |

### 2.1 `.gitignore` 冲突明细

```
本地新增：  /.codebuddy          /.test-build-num
上游新增：  .workbuddy-ai  /test-results*  .codeartsdoer  .kilo  .cursor
```

**解决**：合并为

```gitignore
/.claude
/AGENTS.md
/.workbuddy
.catpaw
.codeartsdoer
.kilo
.cursor
.workbuddy-ai
/test-results*
/.codebuddy
/.test-build-num
```

### 2.2 `tabs.styl` 冲突明细

上游相对本地**删除**了（其实是上游从未有过、而本地新增的美化项）：

- `.tabs` 的 `border-bottom 1px solid var(--main-light)`
- `.tab` / `.tab-close` / `.tabs-add-btn` / `.window-control-box` / `.icon-maximize` 的 5 处 `transition`
- `.active` 的 `box-shadow inset 0 2px 0 0 var(--primary)`

上游**新增**了：

- `.is-touch-device .tab-close { display block }`（触屏无 hover，常显关闭按钮）

**解决**：两边都保留 —— 美化项继续用（§4.1 本来也要重做这些），上游的 `.is-touch-device` 块加入。

> ⚠️ **注意**：上游在 5.5.26 里**主动移除了 tab 的 transition 与 active 高亮**。这说明上游的取向是"减法"。美化方案 §4.1 要重新加回这些，属于**有意的取向差异**，不是冲突，需在 commit message 里说明理由。

> 🔎 **实测更正（影响 §六.2 行号表）**：因为冲突解决时"两边都保留"，`tabs.styl` 合并后是 **321 行**（不是预演的 311 行）。
> 即：**冲突文件不能按上游行数推算，必须读合并后的实际文件**。§六.2 已同步修正。

**实际执行结果**（`git merge upstream/master`，2026-09-23）：冲突文件与预演**完全一致**（仅 `.gitignore` + `tabs.styl`），
`package.json` 自动合并成功且 `btest`/`bdebfast` 均保留；合并统计 **137 A / 220 M / 90 R / 22 D**。

### 2.3 为什么只有 2 个冲突

因为本地相对上游的**真实内容差异极小**：

| 本地独有内容 | 是否冲突 |
| --- | --- |
| `726dffd0` 终端修复 | ❌ 不冲突 —— 其内容与上游 `30fef3fb` **patch-id 完全相同** |
| `ddbd79ef` merge commit | ❌ 不冲突 —— 纯同步产物 |
| `package.json` 的 `btest`/`bdebfast` | ❌ 自动合并 |
| `build/bin/build-deb-fast.js`、`build/bin/build-test.js`、`build/deb-template/*` | ❌ 本地新文件，上游无 |
| `UI-美化设计规范.md` | ❌ 本地新文件 |
| `.gitignore` 追加 | ✅ **冲突 1** |
| `tabs.styl` 追加 | ✅ **冲突 2** |

---

## 三、五个「零变化」—— 升级风险的定心丸 ★

这五项是美化方案的技术地基，**上游一个都没动**：

| # | 项 | 验证方式 | 结果 |
| --- | --- | --- | --- |
| 1 | `css/includes/theme.styl` | `git diff` | **diff 为空**，仍是 15 个变量，**仍无 `--border`** |
| 2 | `store/store.js` 的 `uiThemeConfig` getter | `git diff` | **逐字一致**，`borderRadius: 3` 与 **`motion: false` 仍在** |
| 3 | `common/terminal-theme.js` 的 `requiredThemeProps` | `git diff` | **33 个 key 零增删改** → 旧主题完全兼容 |
| 4 | `main/ui-theme.jsx` | `git diff` | **diff 为空** |
| 5 | `build/vite/conf.js` | `git diff` | **diff 为空**：`target:'esnext'`、`cssCodeSplit:false`、`outDir:work/app/assets` 全一致 |
| 6 | 26 个关键类名 | `git grep` | **全部存在，无删除无改名** |

**第 6 项详情**（已逐一 `git grep` 确认存在）：`.tabs` `.tab` `.tab-close` `.tab-count` `.window-controls` `.sidebar` `.sidebar-panel` `.control-icon-wrap` `.item-list-unit` `.session-wrap` `.type-tab` `.sftp-item` `.sftp-file-prop` `.sftp-history` `.main-footer` `.terminal-footer-unit` `.right-side-panel` `.setting-row-left` `.setting-row-right` `.custom-modal-content` `.custom-modal-header` `.sys-menu` `.layout-menu-item` `.workspace-item` `.term-wrap` `.session-current`

**推论（对美化方案的意义）**：

- §2 的**派生 token 方案**（`--surface-*` 继承 `main-dark/main/main-light`；`--border` 由 `--text` 经 `color-mix` 派生）在 5.5.26 上**原样适用**，因为 15 个变量与派生关系都没变。
- §1.5 的三个主题约束**依然有效**：白名单没变、310 个 iTerm 主题仍是 12 个 ui key、`convertTheme` 逻辑没变。
- §3.4 修 antd 桥接（`borderRadius` 3→8、删 `motion: false`、补 `colorBorder` 等）**依然有效**，且上游**没有修**。
- `color-mix` 的前提（Electron 版本、vite target）**没变**，方案照旧。

---

## 四、必须处理的 6 个变化点

### 4.1 ★ RDP/VNC/SPICE 外壳样式被抽离（唯一需要迁移的条目）

上游新增 `src/client/components/common/remote-session-shell.styl`（+80 行），把 RDP/VNC/SPICE 的公共外壳规则**上移**：

| 文件 | 变化 | 说明 |
| --- | --- | --- |
| `rdp/rdp.styl` | **+13 / -36** | 外壳规则被移除 |
| `vnc/vnc.styl` | **+25 / -16** | 外壳规则被移除 |
| `spice/spice.styl` | **+10 / -19** | 外壳规则被移除 |
| `common/remote-session-shell.styl` | **+80（新）** | 承接上述规则 |

**对方案的影响**：§4.13「RDP / VNC / SPICE 视图」的精修条目中，凡是针对**外壳**（工具栏、容器、全屏包装）的改动，**必须改为作用到 `.remote-session-shell`**，否则改不到实际生效的规则上。

**判断方法**：升级后先读 `remote-session-shell.styl`，确认外壳类名，再决定每条改动落在哪个文件。

### 4.2 测试路径迁移 + 数量翻倍

| 项 | 本地 5.0.6 | 上游 5.5.26 |
| --- | --- | --- |
| e2e 路径 | `test/e2e` | **`src/test/e2e`** |
| e2e spec 数 | 48 | **54**（69 个 .js，含 15 个 common 工具） |
| unit-ci 路径 | `test/unit-ci` | **`src/test/unit-ci`** |
| unit-ci spec 数 | 10 | **30** |

**对方案的影响**：

- §7.3 新增 4 个 CSS 契约单测的落点从 `test/unit-ci/` 改为 **`src/test/unit-ci/`**，写法参考上游 30 个 spec 的约定（需重新确认是 CJS 还是 ESM）。
- `package.json` 的 `test-unit-ci` 脚本路径要同步改。
- §6.9 里的命令全部要改路径。

### 4.3 Playwright 1.28.1 → 1.49.1（e2e 从"跑不起来"变为"可用"）

| 项 | 本地 | 上游 |
| --- | --- | --- |
| playwright | 1.28.1（2022 年，未安装） | **1.49.1** |
| playwright.config | 无 | 仍无 |

**对方案的影响**：§7.4 写的「e2e 现状警告 —— Playwright 太老、与 Electron 42 不兼容、不建议作为门禁」**在升级后作废**。

升级到 1.49.1 后 e2e 有较大概率可用，**可以恢复"e2e 抽查作为区域精修放行条件"**。这是升级带来的一项额外收益。

### 4.4 上游删除了本地专有的构建脚本

上游 `package.json` **没有** `btest`、`bdebfast`、`postpublish` 三个 script（后两者是本地加的，`bdebfast` 是本地 deb 工具链的入口）。

**对方案的影响**：升级后需要**重新加回** `btest` / `bdebfast` 两个脚本（`package.json` 自动合并时可能保留，也可能被覆盖，需实测确认）。若不保留，本地 deb 快速构建链路断掉，§6 里所有 `npm run bdebfast` 命令失效。

> ✅ **实测结果**：`package.json` 自动合并成功，`btest` / `bdebfast` **均保留**，`build/bin/*` 与 `build/deb-template/*` 未被删除。**Step U4 无需执行**。

### 4.5 新增 9 个 `.styl` 需纳入美化范围

详见 §七。

### 4.6 断点变更 + 行号全部重算

- `setting-panel/setting-wrap.styl` 的响应式断点从 **1100px 改为 800px**（方案 §4.7 若涉及该断点需同步）。
- 除 `tree-list.styl` 外，方案给出精确行号的 6 个文件**行号全部失效**（详见 §六.2）。

---

## 五、35 个被改动 `.styl` 的逐文件分类

判据：A = 上游新增（本地无）；B = `+N/-M` 合计 > 30（结构性）；C = ≤ 30（微调）。

### A 类 · 上游新增（9 个）

| 文件 | 行数 | 性质 |
| --- | --- | --- |
| `terminal/shortcut-bar.styl` | +214 | **全新区域**：终端底部快捷键条（新增里最大） |
| `remote-monitor/remote-monitor-bar.styl` | +106 | **全新功能**：远程资源监控条 |
| `remote-monitor/monitor-details.styl` | +87 | **全新功能**：监控详情面板 |
| `common/remote-session-shell.styl` | +80 | **重构抽离**：RDP/VNC/SPICE 公共外壳 |
| `common/item-filter.styl` | +54 | 新通用组件：列表过滤/搜索框 |
| `common/responsive-tabs.styl` | +34 | 新通用组件：响应式标签容器 |
| `ai/ai-history.styl` | +23 | AI 历史面板 |
| `quick-commands/quick-command-ai-editor.styl` | +13 | 快捷命令 AI 编辑器 |
| `common/switch.styl` | +7 | 新通用组件：开关 |

### B 类 · 结构性改动（10 个）

| 文件 | +N/-M | 改动性质 |
| --- | --- | --- |
| `ai/ai.styl` | +110/-6 | **纯追加** AI 聊天区样式，未重构旧规则 |
| `setting-panel/setting-wrap.styl` | +81/-12 | 设置行布局扩展；断点 1100→800px；移动端改双视图 + 面包屑；`left` 改用 `var(--left-side-bar-width,43px)` |
| `css/mobile.styl` | +77/-15 | 触屏定位体系：footer/monitor 改 fixed、侧栏抽屉 z-index 250；大量引入 CSS 变量 |
| `terminal/terminal.styl` | +69/-0 | **纯追加**：`.xterm-decoration-top`、`.terminal-select-text*`（触屏全屏选区） |
| `footer/cmd-history.styl` | +50/-4 | 结构性扩展 |
| `rdp/rdp.styl` | +13/-36 | 外壳规则上移（见 §4.1） |
| `terminal-info/terminal-info.styl` | +9/-34 | 规则裁剪/迁移 |
| `vnc/vnc.styl` | +25/-16 | 外壳规则上移（见 §4.1） |
| `tabs/no-session.styl` | +31/-8 | 结构性扩展 |
| `main/term-fullscreen.styl` | +38/-0 | 纯追加 |

### C 类 · 微调（16 个）

| 文件 | +N/-M | 备注 |
| --- | --- | --- |
| `sidebar/sidebar.styl` | +28/-1 | ⚠️ **数值小但属结构性**：`.sidebar` 改 flex column、新增 `.sidebar-bar`、`.collapsed` 折叠逻辑 |
| `spice/spice.styl` | +10/-19 | ⚠️ 外壳规则上移 |
| `theme/theme-form.styl` | +8/-10 | |
| `tabs/tabs.styl` | +8/-9 | ⚠️ **删除 transition/box-shadow/border-bottom**，新增 `.tab-pin`、`.is-touch-device` |
| `session/session.styl` | +0/-16 | ⚠️ **整块 `.vnc-scroll-wrapper` 被删除**（迁到 vnc.styl） |
| `tree-list/tree-list.styl` | +13/-0 | 仅文末追加 `.is-touch-device` → **原行号稳定** |
| `setting-panel/list.styl` | +12/-0 | 纯追加 |
| `side-panel-r/right-side-panel.styl` | +11/-1 | |
| `footer/footer.styl` | +9/-2 | |
| `common/logo.styl` | +8/-2 | |
| `quick-commands/qm.styl` | +4/-3 | |
| `icons/ai-icon.styl` | +4/-1 | |
| `setting-panel/setting.styl` | +2/-2 | |
| `theme/terminal-theme-list.styl` | +3/-0 | |
| `bookmark-form/common/color-picker.styl` | +3/-0 | |
| `common/drawer.styl` | +1/-1 | |

**无全量重写**：最大的改动是"新增文件"，现有文件中最大是 `ai.styl` 的 +110/-6，且为纯追加。

---

## 六、美化方案逐条目影响矩阵

### 6.1 条目级结论

| 方案条目 | 目标文件 | 上游状态 | 升级后处理 |
| --- | --- | --- | --- |
| §2 Layer 1 派生 token | `css/includes/tokens.styl`（新建） | `css/includes/*` **零新增、theme.styl diff 为空** | ✅ **原样适用**（落点空白，无冲突） |
| §3.1 合并 3 份默认主题副本 | `ui-theme.js` `theme-defaults.js` `theme.styl` | 均未变 | ✅ 原样适用 |
| §3.2 补 `--border` 等死变量 | `sidebar.styl:55` `ai.styl` `cmd-history.styl` | **`--border` 仍未定义**（上游 `sidebar.styl:57`） | ✅ 原样适用（行号 55→57） |
| §3.3 收敛圆角/阴影 | 40+ 文件 | 上游未做 | ✅ 适用（行号重算） |
| §3.4 修 antd 桥接 | `store/store.js` | **逐字未变，`motion:false` 仍在** | ✅ 原样适用 |
| §3.5 动效基线 | 多文件 | ⚠️ 上游 `tabs.styl` **删除了 transition** | ⚠️ 判定为取向差异，保留方案 |
| §3.6 焦点环 | `css/basic.styl` | **`basic.styl` 未变**；上游仍无全局焦点环 | ✅ **原样适用**（落点空白） |
| §3.7 reduced-motion | `css/basic.styl` | 上游仅 3 个文件有，无全局兜底 | ✅ 原样适用 |
| §3.8 CJK 字体 | `css/basic.styl` | 未变 | ✅ 原样适用 |
| §4.1 标签栏 | `tabs.styl` | ⚠️ **冲突文件** + 上游删除 transition/box-shadow | ⚠️ 手工合并；保留双方 |
| §4.2 侧栏 | `sidebar.styl` | ⚠️ 结构性改动（flex column、`.sidebar-bar`、`collapsed`） | ⚠️ 行号 +27 重算；新增折叠态要补样式 |
| §4.3 终端会话区 | `session.styl` `terminal.styl` | ⚠️ `session.styl` 删除 `.vnc-scroll-wrapper`（-16）；`terminal.styl` +69 | ⚠️ **该条目部分作废**；`.vnc-scroll-wrapper` 相关改到 `vnc.styl` |
| §4.4 SFTP | `sftp/*.styl` | ✅ **5 个 sftp .styl 全部未变** | ✅ **完全适用**（仅 `sftp-entry.jsx` 逻辑变，不影响样式） |
| §4.5 底部栏 | `footer.styl` `cmd-history.styl` | footer +9/-2；cmd-history +50/-4 | ⚠️ 行号重算 |
| §4.6 右侧面板 / AI | `right-side-panel.styl` `ai.styl` | right-panel +11/-1；ai.styl +110/-6 | ⚠️ 行号重算；AI 新增区要一并美化 |
| §4.7 设置中心 | `setting-wrap.styl` `list.styl` | ⚠️ +81/-12，断点 1100→**800px** | ⚠️ 行号 +69 重算；断点值同步 |
| §4.8 表单弹窗/树列表 | `bookmark-form.styl` `color-picker.styl` `tree-list.styl` | color-picker +3；tree-list +13（**文末追加，行号稳定**） | ✅ 基本适用 |
| §4.9 通用层 modal/drawer/... | `common/*.styl` | `modal.styl` **未变**；`drawer.styl` +1/-1 | ✅ 基本适用 |
| §4.10 右键菜单 | `sys-menu/sys-menu.styl` | ✅ **未变**（hex 6 处仍在） | ✅ **完全适用** |
| §4.11 快捷命令 | `qm.styl` | +4/-3 微调 | ✅ 基本适用 |
| §4.12 传输/SSH配置/登录页 | `transfer.styl` `login.styl` `layout.styl` … | ✅ 多数未变 | ✅ 基本适用 |
| §4.13 RDP/VNC/SPICE | `rdp/vnc/spice.styl` | ⚠️ **外壳抽到 `remote-session-shell.styl`** | 🔴 **必须迁移作用对象**（§4.1） |
| §4.14 窄屏 | `mobile.styl` | ⚠️ +77/-15，大量新变量 | ⚠️ 行号重算；**新增区域要覆盖** |
| §4.15 零散硬编码清理 | 11 文件 | 上游仍 33 处（本地 32） | ✅ 适用（行号见 §6.2） |

### 6.2 行号偏移表（必须重算的文件）

> ✅ 下表已是**合并后实测行数**（2026-09-23 复核），可直接作为美化方案的行号基准。

| 文件 | 合并后行数 | 上游行数 | 本地旧行数 | 行号可用性 |
| --- | --- | --- | --- | --- |
| `tabs/tabs.styl` | **321** | 311 | 312 | 🔴 需重算（删改分散在 45/49/54/57/82-88/121/125-142/236-237/242 等多处） |
| `sidebar/sidebar.styl` | **161** | 161 | 134 | 🔴 需重算（新增 `.sidebar-bar` / `.collapsed` / `--left-side-bar-width`） |
| `session/session.styl` | **85** | 85 | 101 | 🔴 拆 71 行后全部上移（`.vnc-scroll-wrapper` 已迁走） |
| `terminal/terminal.styl` | **244** | 244 | 175 | 🔴 需重算（`--main-lighter` 相关行 124/143/154/204） |
| `setting-panel/setting-wrap.styl` | **168** | 168 | 99 | 🟡 **局部稳定**：20/26/40/66 行仍有效；新增 70-169 的 800px 媒体查询区 |
| `tree-list/tree-list.styl` | **202** | 202 | 189 | 🟢 **1-189 行稳定**（追加在文末 190-202） |

**其余文件的实测行号**（原方案给出精确行号、本轮已抽验，供修订时直接使用）：

| 文件 | 行数 | 原方案行号是否可用 |
| --- | --- | --- |
| `footer/footer.styl` | 71 | 🔴 `L41-62` → 现 `L46-65`；`L3` 仍有效 |
| `footer/cmd-history.styl` | 119 | 🔴 `L51` → 现 `L65`（结构性扩展） |
| `side-panel-r/right-side-panel.styl` | 46 | 🔴 `L18` → 现 `L28`；`L4/L9` 有效 |
| `ai/ai.styl` | 336 | 🔴 全部失效（127/206→157/236，102/107→132/137，183→213，187→217，190→220，220/223/226→259/262/265） |
| `sys-menu/sys-menu.styl` | 89 | 🟢 12/13/18/19/61/62 全部有效（另发现 `L37` hr 边框、`L45` 阴影） |
| `quick-commands/qm.styl` | 105 | 🟡 `L8/L13` 有效（已是上游变量）；`L99` → `L100` |
| `setting-panel/list.styl` | 71 | 🟢 选中态实测在 `L31-37` |
| `theme/theme-form.styl` | **8** | 🔴 **文件已被清空为纯注释**，原方案的 4 处 `!important` 已不存在 |
| `bookmark-form/common/color-picker.styl` | 21 | 🟢 6/9 有效 |
| `terminal-info/terminal-info.styl` | **14** | 🔴 由 34 行裁到 14 行，原行号全部失效 |

### 6.3 ⚠️ 自造变量撤销 + 上游变量语义澄清（**本轮修正了原建议的一处错误**）

**重要发现**：上游 5.5.26 **自己开始使用 CSS 变量**了：

| 上游变量 | 出处 | 默认值 | **是否动态** |
| --- | --- | --- | --- |
| `--left-side-bar-width` | `main.jsx:173` 写入 `store.leftSideBarWidth + 'px'`；`sidebar.styl:104`、`qm.styl:8`、`footer.styl:5`、`setting.styl:10`、`setting-wrap.styl:75`、`drawer.styl:4` 消费 | `43px` | 否（但随用户拖拽变） |
| `--footer-stack-height` | `main.jsx:174` 写入 `footerHeight + (监控条? 高度:0)`；`qm.styl:13`、`mobile.styl:120`、`term-fullscreen.styl:69,75` 消费 | `36px` | **是**：footer + 远端监控条合计高度 |
| `--shortcut-bar-h` | `shortcut-bar.jsx:160` 写入；`shortcut-bar.styl:11`、`mobile.styl`、`term-fullscreen.styl` 消费 | `44px`（移动端视角 `0px`） | **是** |
| `--shortcut-bar-kb-offset` | `shortcut-bar.jsx:148` 写入 | `0px` | **是**（软键盘偏移） |

**因此方案 §2.2 里自造的 `--h-bar`(36px) 与 `--w-sidebar`(36px) 应当撤销**：

| 方案原命名 | 处置 | 理由 |
| --- | --- | --- |
| `--h-bar` | ❌ **不替换**，保持原数值 `36px` | 原方案想收纳的 5 处（tabs 高 / `.main-footer` 高 / `.session-wrap` padding-top / `.right-side-panel` top / `.sidebar-panel` top）**语义各不相同**，其中**没有一处等于 `--footer-stack-height`**。见下方警告。 |
| `--w-sidebar` | ❌ **不替换**，保持原数值 `36px` | `.sidebar{width:36px}` 是图标栏宽度，而 `--left-side-bar-width` 实测为 **43px**（`constants.js:40 sidebarWidth = 43`，且随拖拽变化）。用 `var(--left-side-bar-width, 36px)` 替换 `.sidebar` 的宽度会在变量已赋值时**把 36px 变成 43px → 布局位移 7px**。 |

> 🔴 **警告（原 §6.3 建议有误，务必按此执行）**：**不要把** `--h-bar` 简单映射为 `var(--footer-stack-height, 36px)`。
> `--footer-stack-height` 是 **footer + 远端监控条** 的合计高度（`main.jsx:174`），是**动态值**：
> - 用在 `.main-footer{height:36px}` 上 → 打开远端监控条时 footer 会被撑高 → **布局变化**；
> - 用在 `.session-wrap{padding-top:36px}` / `.right-side-panel{top:36px}` 上 → 语义完全不符（那是"标签栏高度"）。
>
> **正确做法**：这些 `36px` **一律保持数值不变**（安全边界优先，语义化收纳的收益不值得引入布局风险）。
> 只有**上游自己已经写成 `var(...)` 的 6 处 `--left-side-bar-width` 消费点**保持原样不动。

**其余新增变量名**（`--surface-*` `--border` `--border-strong` `--hover-bg` `--active-bg` `--mask` `--radius-*` `--shadow-*` `--sp-*` `--fs-*` `--dur-*` `--ease`）与上游**无命名冲突**，可保留。
**原方案的 `--focus-ring` 亦建议撤销**（上游已有两种焦点写法，见 §十.14 与 §七）。

---

## 七、新增 9 个区域的样式特征与纳入建议

上游新代码**已经比老代码规范**（大量用 `var()`、几乎无硬编码色），但**仍未 token 化圆角/阴影**：

| 文件 | `var()` 用量 | hex | `border-radius` 散值 | `box-shadow` 散值 | `focus-visible` | reduced-motion | 纳入建议 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `terminal/shortcut-bar.styl` | 33 | 0 | **5 处** | 0 | 否 | 否 | 🟠 **重点纳入**（新区域 + 最多散值） |
| `remote-monitor/monitor-details.styl` | 11 | 0 | 0 | 0 | 否 | **是** | 🟢 补 focus 即可 |
| `remote-monitor/remote-monitor-bar.styl` | 11 | 0 | 0 | **2 处** | **是** | **是** | 🟢 阴影 token 化 |
| `common/remote-session-shell.styl` | 6 | 0 | **1 处** | **1 处** | 否 | 否 | 🟠 纳入（承接 RDP/VNC/SPICE 外壳） |
| `common/item-filter.styl` | 8 | 0 | 0 | 0 | **是** | 否 | 🟢 参考样板（上游唯一做了焦点的组件） |
| `common/responsive-tabs.styl` | 5 | 0 | **1 处** | 0 | 否 | 否 | 🟡 轻量纳入 |
| `ai/ai-history.styl` | **0** | 0 | 0 | 0 | 否 | 否 | 🟠 **重点纳入**（完全未用变量） |
| `quick-commands/quick-command-ai-editor.styl` | **0** | 0 | 0 | 0 | 否 | 否 | 🟠 **重点纳入**（完全未用变量） |
| `common/switch.styl` | **0** | 0 | 0 | 0 | 否 | 否 | 🟠 **重点纳入**（完全未用变量，开关是对比度敏感组件） |

**结论**：

- 新区域**没有硬编码颜色问题**，但 `switch.styl` / `ai-history.styl` / `quick-command-ai-editor.styl` **完全没用 CSS 变量**，`shortcut-bar.styl` 有 5 处圆角散值（L62/L123/L138/L154/L175，均为 `6px`/`4px`） → 这 4 个是**新增美化重点**。
- 🔎 **实测更正（§十.14）**：上游**不是只有一种**焦点写法，而是**两种并存**：

  | 写法 | 出处 | 形式 |
  | --- | --- | --- |
  | **描边式**（列表/容器类） | `common/item-filter.styl:16-18`、`:38-40` | `outline 1px solid var(--primary)` + `outline-offset 1px`（另一处为 `-1px`） |
  | **内阴影式**（行内小控件） | `remote-monitor/remote-monitor-bar.styl:43-46`、`:83-93` | `box-shadow inset 0 0 0 1px var(--primary)` |

  → 美化方案 §3.6 的全局基线**应采用「描边式」**（`outline 1px solid var(--primary)` + `outline-offset 1px`），与通用列表组件 `item-filter` 一致；
  **撤销原方案自造的 `--focus-ring` 阴影 token**，避免造成第三套焦点语言。
- 🔎 **另一处更正**：原方案把 `tree-list/tree-list.styl:25` 的 `outline 1px dashed var(--primary)` 说成"全库唯一保留的焦点指示"——**误读**。
  它在 `&.search-selected` 规则内（L24-25），是**搜索命中高亮**，与键盘焦点无关。
  同理 `modal.styl:8`（`.custom-modal-wrap` 容器）、`rdp.styl:15`（`.rdp-canvas`）、`basic.styl:26`（`::-webkit-scrollbar-thumb`）、
  `terminal.styl:106`（`.terminal-select-text-area`）、`ai.styl:109`（`.ai-attachment-dragover`）的 `outline` 也**都不是**焦点相关；
  全库真正"移除焦点轮廓"的只有 `modal.styl:68-69`（`&:focus { outline none }`）与 `remote-monitor-bar.styl:35,92`。
- 新区域整体应**纳入方案第 16-17 个精修区域**，排在老区域之后（老区域收益更大）。

---

## 八、上游「已确认问题」复核（美化价值是否仍成立）

| 我在方案里指出的问题 | 上游 5.5.26 状态 | 结论 |
| --- | --- | --- |
| `--border` 被引用但未定义 | **仍存在**：`theme.styl` 的 `:root` 无 `--border`；上游 `sidebar.styl:57` 仍引用它（仅 2 处 jsx 用 `var(--border,#333)` 兜底） | ✅ **美化价值成立** |
| 硬编码 hex | **33 处**（本地 32）。实测分布：`ai.styl` 9、`sys-menu.styl` 6、`tabs.styl` 6、`tree-list.styl` 3、`term-search.styl` 2、`color-picker.styl` 2、`terminal.styl`/`info.styl`/`remote-float-control.styl`/`drag-handle.styl`/`login.styl` 各 1 | ✅ 成立（几乎没改善，`tabs.styl` 反而 +2） |
| `!important` | **46 处**（本地 45）。实测分布：`term-fullscreen.styl` **19**、`mobile.styl` 8、`spice.styl` 7、`sftp.styl` 3、`terminal.styl` 3、`cmd-history.styl`/`qm.styl`/`tabs.styl`/`sidebar.styl`/`vnc.styl`/`bookmark-form.styl` 各 1 | ✅ 成立（**反而变多**） |
| 无全局键盘焦点环 | 全库仅 `item-filter.styl`(3)、`remote-monitor-bar.styl`(4) 两文件用 `focus-visible`；**未建立全局焦点环** | ✅ **成立**（上游只在新组件局部处理） |
| 无全局 `prefers-reduced-motion` | 仅 3 个文件：`bookmark-form.styl:62`、`monitor-details.styl:85`、`remote-monitor-bar.styl:104` | ✅ 成立 |
| 设计 token 太少 | `theme.styl` 仍是 **15 个纯颜色变量** | ✅ **成立**（上游零改进） |
| antd 桥接不完整 | `uiThemeConfig` getter **逐字未变**（现 `store.js:266-283`，`borderRadius: 3` @L271 + `motion: false` @L280） | ✅ **成立** |

**★ 新增指控（本轮实测发现，v2 方案未覆盖）**：

| 问题 | 证据 | 影响 |
| --- | --- | --- |
| 🔴 **`darker()` 输出非法颜色** | `main/ui-theme.jsx` L11-29 缺上界钳制。实测：`darker('#121214',0.3) = '#0'`（2 字符）；`darker('#ededed',-0.3) = '#1393939'`（8 字符）；`darker('#ffffff',-0.3) = '#14b4b4b'` | **默认深色主题下 29 处 `var(--main-darker)` 全部失效**；浅色 iTerm 主题下 26 处 `var(--main-lighter)` 失效。<br>更糟的是：运行时注入的 `#theme-css` 在产物 HTML 中位于 `<link rel=stylesheet>` **之后**（`work/app/assets/index.html` 实测），**覆盖掉 `theme.styl:15-16` 那组合法静态兜底值** `#0e0d0e`/`#5b5a5b`。 |

> 该 bug **强化了美化方案 §2「用 `color-mix` 派生 token」的必要性**：`--border` / `--hover-bg` 等新 token 必须由 `--text`/`--primary` 派生，**不得复用 `--main-darker`**。
> 同时应在 §3 增加一条**最小修复**：给 `darker()` 补 `>255` 钳制并统一补零到 6 位（3 行代码，独立提交、可回滚）。

**总结论**：上游 5.5.26 在**功能**上大幅前进（215 个提交），但在**样式体系**上**几乎没有改进**，我方案的 7 项核心指控全部依然成立。升级不会让美化白做。

**唯一需要重新判断的**：上游 `tabs.styl` 主动删除了 transition 与 active 高亮。这不代表方案错，而是上游在"减法"。方案 §4.1 保留这两个美化项，但**要在 commit message 中说明这是有意的取向差异**，便于将来与上游对比时不会误判为回归。

---

## 九、升级执行方案

### 9.1 方式选择

| 方式 | 命令 | 优点 | 缺点 | 推荐 |
| --- | --- | --- | --- | --- |
| **A. 合并** | `git merge upstream/master` | 非破坏性；保留双方历史；与用户已有的 `ddbd79ef` merge 做法一致 | 产生 1 个大 merge commit | ✅ **推荐** |
| B. 变基 | `git rebase --rebase-merges upstream/master` | 线性历史 | 本地有 merge commit，变基复杂易错；破坏性 | ❌ 不推荐 |
| C. 重置 + 重放 | `git reset --hard upstream/master` + cherry-pick 本地提交 | 历史最干净 | **破坏性**（需明确授权）；本地的 deb 工具链、tabs 改动需重新落地 | ⚠️ 备选 |

**推荐 A**。本地相对上游只有 2 个文件冲突、且都是"双方各自追加"，合并成本极低。

### 9.2 执行步骤（**已执行，含实测结果**）

#### Step U0 · 安全备份 ✅

```bash
git branch backup/before-5.5.26-upgrade   # = 787b8eb5
git push github backup/before-5.5.26-upgrade
```

> ✅ 已推 fork，异地备份就位。**升级前工作区干净**（`git status --short` 为空）。

#### Step U1 · 再次确认冲突面 ✅

```bash
git merge-tree --write-tree --name-only HEAD upstream/master
# 实测输出：.gitignore 与 src/client/components/tabs/tabs.styl
```

#### Step U2 · 执行合并 ✅

```bash
git merge upstream/master
# 实测：仅上述 2 个文件报冲突，package.json 等全部自动合并
# 合并统计：137 added / 220 modified / 90 renamed / 22 deleted
```

#### Step U3 · 解决 2 个冲突 ✅

`tabs.styl` 按"双方各追加取并集"解决：保留本地 3 项美化（`border-bottom` + 5 处 `transition` + `inset 0 2px` 高亮）
+ 上游 `.is-touch-device` 块 → 合并后 **321 行**。

#### Step U4 · 恢复本地专有构建脚本 ✅（**无需操作**）

实测 `package.json` 自动合并后 `btest` / `bdebfast` 仍在，`build/bin/build-deb-fast.js`、`build/bin/build-test.js`、`build/deb-template/*` 均未被删除。

#### Step U5 · 验证 ✅（**注意：本步骤的环境要求与原文档不符，以下为实测有效做法**）

```bash
# ⚠️ 必须用 Node 22，系统 Node 20.15.1 跑不了本项目工具链（§十三）
export PATH="$HOME/.config/nvm/versions/node/v22.23.2/bin:$PATH"

npm install --legacy-peer-deps   # ⚠️ 必须加该 flag（vite@8.1.0 的 peer 与项目 pin 的 @types/node 冲突）
npm run lint                     # ✅ 通过
npm run compile                  # ✅ 通过（仅剩既有 cp 告警，见 §十三）
npm run test-unit-ci             # ✅ 450/450 通过（路径已自动变为 src/test/unit-ci/*.spec.js）
npm run t                        # ✅ 应用可启动
```

**验证清单结果**：

- [x] 应用能启动，无白屏
- [x] 设置中心能打开，主题能切换
- [x] 切到 iTerm 主题（含浅色）正常
- [x] 终端能连接
- [ ] RDP/VNC/SPICE 视图 —— **未逐一实测**（无可用远端），改为代码层核对类名（§4.1 已完成）
- [ ] 远端监控条、快捷键条（新功能）—— **未逐一实测**，已纳入美化方案 §4.16/§4.17

#### Step U6 · 提交与推送 ✅

```bash
git commit        # c9029f9c "Merge upstream/master into master（升级 5.0.6 → 5.5.26）"
git push github master   # gitee 由镜像自动同步
```

### 9.3 回滚方式（保留）

```bash
# 合并未提交时
git merge --abort
# 已提交后
git reset --hard backup/before-5.5.26-upgrade   # = 787b8eb5
git push github master --force-with-lease       # ⚠️ 需明确确认才执行
```

> 备份分支 `backup/before-5.5.26-upgrade` 保留至美化全部完成，**不要删除**。

---

## 十、升级后的美化方案修订清单（**已于 2026-09-23 全部执行完毕**）

`UI-美化设计规范.md` 已按本清单修订为 **v3**。执行情况：

| # | 修订项 | 涉及章节 | 执行结果 |
| --- | --- | --- | --- |
| 1 | 删除自造变量 `--h-bar` / `--w-sidebar` | §2.2、§2.3、§4.x | ✅ 已撤销；并**修正**为"这些 36px 一律不替换、保持原值"（§6.3 警告） |
| 2 | 全部行号按升级后代码重算 | §3.x、§4.x | ✅ 6 个必算文件 + 10 个抽验文件全部按**合并后实测行号**更新（§6.2） |
| 3 | §4.13 作用对象迁到 `common/remote-session-shell.styl` | §4.13 | ✅ 已迁移；类名按实测：`.remote-session-wrap`(`.fit`)、`-spin`、`-control`(`-left`/`-right`)、`-viewport`、`-fill` |
| 4 | §4.3 中 `.vnc-scroll-wrapper` 条目迁到 `vnc.styl` | §4.3 | ✅ 已迁移并标注"该块已从 `session.styl` 删除，现位于 `vnc.styl:17-22`" |
| 5 | §4.7 断点值 1100px → 800px | §4.7 | ✅ 已同步（`setting-wrap.styl:70`） |
| 6 | 新增精修区域（shortcut-bar / remote-monitor / item-filter / switch / ai-history / responsive-tabs / quick-command-ai-editor） | §四 | ✅ 已新增 **§4.16 / §4.17** 两个区域（含 9 个上游新文件） |
| 7 | §1.3 文件清单 58 → 67 | §1.3 | ✅ 已改为 67 并补全新增清单 |
| 8 | §1.4 测试现状更新（`src/test/`、54 e2e、30 unit-ci、playwright 1.49.1） | §1.4、§7 | ✅ 已更新 |
| 9 | §7.4「e2e 不可用」警告删除，改为"可用，建议抽查放行" | §7.4 | ✅ 已改写 |
| 10 | §6.9 单测落点改为 `src/test/unit-ci/` | §6.9 | ✅ 已改（并核实为 CJS + `node:test`，与现有 30 个 spec 一致） |
| 11 | §5.4 对比度结论重新实测 | §5.4 | ✅ 已重测，并新增 3 组**新 UI** 的实测（`shortcut-bar` 主色实心、`setting-mobile-back-btn` 主色实心、`ai.styl` 残留 `#1890ff`） |
| 12 | §5.5 性能结论重新评估（`!important` 19 处） | §5.5 | ✅ 已更新为 46 处总量，`term-fullscreen.styl` 单文件 19 处 |
| 13 | §4.1 补充"上游删除 transition/active 高亮，本方案有意恢复" | §4.1 | ✅ 已加"取向差异"说明块 |
| 14 | §3.6 对齐上游 `focus-visible` 写法 | §3.6 | ✅ 已改为上游**描边式**（`outline 1px solid var(--primary)` + `outline-offset 1px`）；**同时撤销 `--focus-ring` token**，并更正了原方案对 5 处 `outline` 移除的误读（§七） |

**本轮新增的 3 项修订**（不在原 14 项内，由实测发现）：

| # | 修订项 | 章节 |
| --- | --- | --- |
| 15 | 写入 `darker()` 非法颜色 bug（29 处 `--main-darker` 失效），并新增 §3.9 最小修复步骤 | §1.2、§3.9 |
| 16 | `.main-footer` 补上边框改用 `box-shadow inset`（`.main-footer` 是 content-box，加 `border-top` 会外扩 1px = 布局位移） | §4.5 |
| 17 | `setting/list.styl:31-37`、`shortcut-bar.styl:70-71` 的"主色实心 + 白字"纳入对比度整改（3.89:1，AA 不达标） | §4.7、§4.16、§5.4 |

---

## 十一、风险登记册

| # | 风险 | 概率 | 影响 | 缓解 |
| --- | --- | --- | --- | --- |
| U1 | 升级后应用无法启动（479 文件变更） | 低 | 高 | Step U0 备份分支；Step U5 完整验证；可整体回滚 |
| U2 | `npm install` 后依赖冲突（playwright 等大版本跳跃） | 中 | 中 | 升级后先 `npm install` 再编译；失败可回滚备份分支 |
| U3 | `tabs.styl` 冲突解决时误删上游 `.is-touch-device` | 中 | 低 | 按 §2.2 明确清单逐条核对 |
| U4 | 本地 deb 工具链脚本丢失，构建链路断 | 中 | 中 | Step U4 专门恢复并验证 `grep` |
| U5 | 升级后美化方案行号全错，改动落错位置 | **高** | 中 | §十.2 强制重算；先升级验证通过再改文档最后动样式 |
| U6 | 上游新增区域带来新的对比度/一致性问题未纳入方案 | 中 | 低 | §十.11 要求在升级后重新实测对比度 |
| U7 | 用户自建主题在升级后失效 | **极低** | 高 | 已核实 `requiredThemeProps` 33 key 零变化 |
| U8 | 310 个 iTerm 主题在升级后异常 | **极低** | 低 | 已核实主题加载链路未变 |
| U9 | 升级引入上游的 `!important`（46 处 > 本地 45）导致清理工作量增加 | 高 | 低 | 方案 §5.3 目标值已从"≤16 处"上调为 **"≤20 处"**（保留 19 + reduced-motion 1），并分批清理 |

---

## 十二、执行顺序与状态

```
Step U0  打备份分支 + 推 fork                    ✅ 完成（backup/before-5.5.26-upgrade = 787b8eb5）
Step U1  merge-tree 复核冲突面                   ✅ 完成（2 个冲突，与预演一致）
Step U2  git merge upstream/master               ✅ 完成（137A/220M/90R/22D）
Step U3  解决 2 个冲突（.gitignore / tabs.styl）  ✅ 完成
Step U4  恢复 deb 构建工具链脚本                  ✅ 无需操作（自动保留）
Step U5  验证：install → lint → compile → 单测    ✅ 完成（450/450，Node 22 + --legacy-peer-deps）
Step U6  提交 + 推送 fork（gitee 自动镜像）       ✅ 完成（c9029f9c）
   ↓
Step A   按 §十 修订 UI-美化设计规范.md           ✅ 完成（v3，17 项）
   ↓
Step B   按修订后的规范执行美化                   ⏳ **待执行**（约 30 步，见规范 §6）
```

**关键原则（保留）**：每步验证未通过前，不进入下一步；美化期间保持"一步一 commit，可独立 revert"。

---

## 十三、环境要求与实测基线（★ 美化执行前必读）

升级后在**.当前这台机器**上跑通全链路，需要绕过 3 个环境坑。以下均为实测结论，**未修改 `package.json`**（避免与上游分叉）。

### 13.1 Node 版本：必须 22（系统 Node 20 跑不通）

| 项 | 值 |
| --- | --- |
| 系统 Node | `v20.15.1` ← **跑不了本项目工具链** |
| 可用 Node | `v22.23.2`（nvm 安装） |
| 激活方式 | `export PATH="$HOME/.config/nvm/versions/node/v22.23.2/bin:$PATH"` |

> ⚠️ **每条构建/测试命令前都要加这一行**（Shell 不保留状态）。

### 13.2 依赖安装：`npm install` 必须带 `--legacy-peer-deps`

| 现象 | 原因 |
| --- | --- |
| `npm install` 或 `npm ci` 报 peer 冲突失败 | `vite@8.1.0` 的 peer 要求 `@types/node ^20.19.0 \|\| >=22.12.0`，而项目 pin 了 `22.9.3`；**上游 lock 文件自身也不一致** |

```bash
npm install --legacy-peer-deps
```

> 该 flag **不写入仓库**（`package.json`/`.npmrc` 保持与上游一致），仅在本地安装时手工添加。

### 13.3 Electron 二进制镜像（用户级 `~/.npmrc`）

```
electron_mirror=https://npmmirror.com/mirrors/electron/
```

> 写入**用户级** `~/.npmrc`，**不进仓库**。若后续 `node_modules` 被清空重装，需保留该配置，否则 electron 二进制下载会失败。

### 13.4 常用命令与产物（**行号/文件名以此为准**）

| 命令 | 作用 | 实测耗时量级 |
| --- | --- | --- |
| `npm run compile` | 仅 vite 编译 → `work/app/assets/` | 秒级 |
| `npm run t` | `electron work/app/app.js` 直接启动 | 秒级 |
| `npm run test-unit-ci` | `node --test src/test/unit-ci/*.spec.js` | 秒级 |
| `npm run lint` | `standard --verbose`（只扫 JS，不扫 `.styl`） | 秒级 |
| `npm run bdebfast` | 自增号 → compile → prepare → asar → `fakeroot dpkg-deb` → `dist/electerm-5.5.26-test.N-linux-amd64.deb` | 数十秒 |

**编译产物（美化文档中所有 grep 验证都要用这个名字）**：

```
work/app/assets/css/style-5.5.26.css      # 78,359 字节，单行压缩
```

> ⚠️ 旧产物 `work/app/assets/css/style-5.0.6.css` 仍在目录里（61,191 字节），**是升级前的遗留物，不要拿它验证**。

**已知的、无害的编译告警**（升级前就有，可忽略）：

```
cp: cannot stat '.../@electerm/electerm-resource/tray-icons/*': No such file or directory
```

该包 2.2.1 起就没有 `tray-icons` 目录。

### 13.5 git 现状（美化开工前的基线）

| 项 | 值 |
| --- | --- |
| `HEAD` | `c9029f9c`（merge commit） |
| 工作区 | **干净**（`git status --short` 为空） |
| 备份分支 | `backup/before-5.5.26-upgrade` = `787b8eb5` |
| 远程 | `github`（fork，镜像到 gitee）、`origin`（gitee）、`upstream`（官方） |
| `pre-push` 钩子 | 会跑 `npm run lint` → 推送前必须先过 lint |

### 13.6 测试基线

| 层 | 路径 | 规模 | 状态 |
| --- | --- | --- | --- |
| unit-ci | `src/test/unit-ci/*.spec.js` | **30 spec / 450 test** | ✅ **450/450 全过**（Node 22） |
| e2e | `src/test/e2e/*.spec.js` | **54 spec** | 🟡 playwright 已升至 1.49.1，**具备可用条件**，待实际抽查确认 |
| integration | `src/test/integration/*.spec.js` | 2 spec | 需活体 MCP server，自 skip |
| 样式断言 | — | **0 处** | 纯样式改动不会打破断言（安全边界的依据） |

> 写新增单测时参考现有 spec 的写法：**CommonJS + `require('node:test')`**，纯文本分析、不依赖 DOM/浏览器/构建产物。